// Define the type alias
type GeneralLinksConfig = {
  purchaseLink: string;
  faqLink: string;
};

// Declare the constant with the specific type
const GENERAL_LINKS_CONFIG: GeneralLinksConfig = {
  purchaseLink: '',
  faqLink: '',
};

export { GENERAL_LINKS_CONFIG };
